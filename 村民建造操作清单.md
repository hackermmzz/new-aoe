# 村民建造操作清单

本文档列出**村民可建造的所有建筑**，以及每项建造的**时代要求、前置建筑、资源消耗**和**代码实现位置**。数值以 `config.json` 为准（若未在 config 中配置则使用 `GlobalVariate.cpp` 的默认值）。

---

## 一、时代与常量对应

| 时代枚举 | 含义 |
|----------|------|
| `CIVILIZATION_STONEAGE` (1) | 石器时代 |
| `CIVILIZATION_TOOLAGE` (2)   | 工具时代 |
| `CIVILIZATION_BRONZEAGE` (3)| 铜器时代 |
| `CIVILIZATION_IRONAGE` (4)  | 铁器时代 |

- **是否显示/可造**：`Development::get_isBuildingShowAble(buildingNum, civilization)` → `developLab[buildingNum].buildCon->isShowable(civilization)`  
- **资源是否足够**：`Development::get_isBuildingAble(buildingNum, wood, food, stone, gold)` → `developLab[buildingNum].buildCon->executable(...)`  
- **扣资源**：`Player::changeResource_byBuild(buildNum)` → `playerScience->get_Resource_Consume(buildNum, wood, food, stone, gold)`，再 `changeResource(..., true)`  
- **科技树定义**：[Development.cpp](Development.cpp) `init_DevelopLab()` 中为每个 `BUILDING_*` 设置 `developLab[].buildCon`（`conditionDevelop`），内含时代、时间、资源消耗及前置条件。

---

## 二、村民可建造建筑总表

| 序号 | 建筑名称 | 枚举常量 | 时代要求 | 前置条件 | 木材 | 食物 | 石头 | 金子 | 建造时间(秒) | 代码位置（科技树/资源） |
|------|----------|----------|----------|----------|------|------|------|------|--------------|--------------------------|
| 1 | 市镇中心 | BUILDING_CENTER | 铁器时代 | 无 | 200 | 0 | 0 | 0 | 60 | [Development.cpp](Development.cpp) 397 行；[config.json](config.json) BUILD_CENTER_WOOD=200, TIME_BUILD_CENTER=60 |
| 2 | 房屋 | BUILDING_HOME | 石器时代 | 无 | 30 | 0 | 0 | 0 | 20 | [Development.cpp](Development.cpp) 419 行；BUILD_HOUSE_WOOD=30, TIME_BUILD_HOME=20 |
| 3 | 仓库 | BUILDING_STOCK | 石器时代 | 无 | 120 | 0 | 0 | 0 | 30 | [Development.cpp](Development.cpp) 423 行；BUILD_STOCK_WOOD=120, TIME_BUILD_STOCK=30 |
| 4 | 谷仓 | BUILDING_GRANARY | 石器时代 | 无 | 120 | 0 | 0 | 0 | 30 | [Development.cpp](Development.cpp) 466 行；BUILD_GRANARY_WOOD=120, TIME_BUILD_GRANARY=30 |
| 5 | 军营 | BUILDING_ARMYCAMP | 石器时代 | 无 | 125 | 0 | 0 | 0 | 30 | [Development.cpp](Development.cpp) 480 行；BUILD_ARMYCAMP_WOOD=125, TIME_BUILD_ARMYCAMP=30 |
| 6 | 船坞 | BUILDING_DOCK | 石器时代 | 无 | 100 | 0 | 0 | 0 | 40 | [Development.cpp](Development.cpp) 656 行；BUILD_DOCK_WOOD=100, TIME_BUILD_DOCK=40 |
| 7 | 市场 | BUILDING_MARKET | 工具时代 | 已建谷仓 | 150 | 0 | 0 | 0 | 40 | [Development.cpp](Development.cpp) 515-516 行；buildCon->addPreCondition(谷仓)；BUILD_MARKET_WOOD=150, TIME_BUILD_MARKET=40 |
| 8 | 靶场 | BUILDING_RANGE | 工具时代 | 已建军营 | 150 | 0 | 0 | 0 | 40 | [Development.cpp](Development.cpp) 607-608 行；addPreCondition(军营)；BUILD_RANGE_WOOD=150, TIME_BUILD_RANGE=40 |
| 9 | 马厩 | BUILDING_STABLE | 工具时代 | 已建军营 | 150 | 0 | 0 | 0 | 40 | [Development.cpp](Development.cpp) 577-578 行；addPreCondition(军营)；BUILD_STABLE_WOOD=150, TIME_BUILD_STABLE=40 |
| 10 | 箭塔 | BUILDING_ARROWTOWER | 工具时代 | 谷仓已研发“箭塔” | 0 | 0 | 150 | 0 | 80 | [Development.cpp](Development.cpp) 648-649 行；addPreCondition(谷仓 actCon[BUILDING_GRANARY_ARROWTOWER].headAct)；BUILD_ARROWTOWER_STONE=150, TIME_BUILD_ARROWTOWER=80 |
| 11 | 农田 | BUILDING_FARM | 工具时代 | 已建市场 | 75 | 0 | 0 | 0 | 30 | [Development.cpp](Development.cpp) 644-645 行；addPreCondition(市场)；BUILD_FARM_WOOD=75, TIME_BUILD_FARM=30 |
| 12 | 攻城武器厂 | BUILDING_SIEGE | 铜器时代 | 无 | 180 | 0 | 0 | 0 | 40 | [Development.cpp](Development.cpp) 674 行；BUILD_SIEGE_WOOD=180, TIME_BUILD_SIEGE=40 |
| 13 | 学院 | BUILDING_COLLAGE | 铜器时代 | 无 | 180 | 0 | 0 | 0 | 40 | [Development.cpp](Development.cpp) 682 行；BUILD_COLLAGE_WOOD=180, TIME_BUILD_COLLAGE=40 |

说明：

- **建造时间**：对应 `TIME_BUILD_*`，单位为“秒”（或与帧率换算的时长），在 `conditionDevelop` 的 `times_second` 中使用。  
- **前置条件**：在代码中通过 `developLab[建筑].buildCon->addPreCondition(另一建筑 buildCon 或 actCon[某行动].headAct)` 实现；游戏内“是否显示/可造”会递归检查前置是否满足。  
- BUILDING_TEMPLE、BUILDING_WALL 在枚举中存在但当前未在 `init_DevelopLab()` 中实现，村民无法建造。

---

## 三、按时代分类

### 石器时代（CIVILIZATION_STONEAGE）

| 建筑 | 木材 | 前置 |
|------|------|------|
| 房屋 | 30 | 无 |
| 仓库 | 120 | 无 |
| 谷仓 | 120 | 无 |
| 军营 | 125 | 无 |
| 船坞 | 100 | 无 |

### 工具时代（CIVILIZATION_TOOLAGE）

| 建筑 | 木材 | 石头 | 前置 |
|------|------|------|------|
| 市场 | 150 | — | 谷仓 |
| 靶场 | 150 | — | 军营 |
| 马厩 | 150 | — | 军营 |
| 箭塔 | — | 150 | 谷仓“研发箭塔”已完成 |
| 农田 | 75 | — | 市场 |

### 铜器时代（CIVILIZATION_BRONZEAGE）

| 建筑 | 木材 | 前置 |
|------|------|------|
| 攻城武器厂 | 180 | 无 |
| 学院 | 180 | 无 |

### 铁器时代（CIVILIZATION_IRONAGE）

| 建筑 | 木材 | 前置 |
|------|------|------|
| 市镇中心 | 200 | 无 |

---

## 四、代码实现清单（村民建造流程）

1. **AI 下达建造指令**  
   - `UsrAI::HumanBuild(int SN, int BuildingNum, int BlockDR, int BlockUR)` → [AI.cpp](AI.cpp) 19-20 行  
   - `AddToIns(instruction(INS_HUMANBUILD, SN, BlockDR, BlockUR, BuildingNum))`

2. **每帧处理指令**  
   - `Core::manageOrder(id)` 中 `case INS_HUMANBUILD`：[Core.cpp](Core.cpp) 约 1289-1320 行  
   - 校验：self 为村民、农民类型为 FARMERTYPE_FARMER、BlockDR/BlockUR 合法、BuildingNum 合法  
   - 调用：`interactionList->addRelation(self, cur.BlockDR, cur.BlockUR, CoreEven_CreatBuilding, true, cur.option)`

3. **玩家点击建造按钮**  
   - `SelectWidget::getBuild(BlockL, BlockU, num)`：[SelectWidget.cpp](SelectWidget.cpp) 约 1329-1332 行  
   - `core->addRelation(nowobject, BlockL, BlockU, CoreEven_CreatBuilding, true, num)`

4. **关系表添加建造**  
   - `Core_List::addRelation(object1, BlockDR, BlockUR, eventType, respond, type)`：[Core_List.cpp](Core_List.cpp) 约 220-270 行  
   - 校验：object1 为村民；`is_BuildingCanBuild(type, BlockDR, BlockUR, ...)`（越界/未探索/重叠/地形/陆地）；  
   - `player[playerRepresent]->get_isBuildingShowAble(type)`（时代+前置）；  
   - `player[playerRepresent]->get_isBuildingAble(type)`（资源）；  
   - `player[playerRepresent]->changeResource_byBuild(type)` 扣资源；  
   - `player[playerRepresent]->addBuilding(type, BlockDR, BlockUR)` 创建地基；  
   - `addRelation(object1, buildOb, CoreEven_FixBuilding, respond)` 村民去修建。

5. **资源消耗从哪里读**  
   - `Player::changeResource_byBuild(int buildNum)`：[Player.cpp](Player.cpp) 253-259 行  
   - `playerScience->get_Resource_Consume(buildNum, wood, food, stone, gold)`  
   - `Development::get_Resource_Consume(int buildNum, int& wood, ...)`：[Development.h](Development.h) 94 行  
   - 内部：`developLab[buildNum].buildCon->get_needResource(wood, food, stone, gold)`  
   - 数值来源于 `conditionDevelop` 构造时传入的全局常量（如 BUILD_*_WOOD），这些常量从 [config.json](config.json) 或 [GlobalVariate.cpp](GlobalVariate.cpp) 加载。

6. **是否显示/可造**  
   - `Player::get_isBuildingShowAble(buildNum)` → `playerScience->get_isBuildingShowAble(buildNum, getCiv())`：[Player.h](Player.h) 84 行  
   - `Development::get_isBuildingShowAble(buildingNum, civilization)` → `developLab[buildingNum].buildCon->isShowable(civilization)`：[Development.h](Development.h) 87 行  
   - `Player::get_isBuildingAble(buildNum)` → `playerScience->get_isBuildingAble(buildNum, wood, food, stone, gold)`：[Player.h](Player.h) 85 行  
   - `Development::get_isBuildingAble(...)` → `developLab[buildingNum].buildCon->executable(wood, food, stone, gold)`：[Development.h](Development.h) 88-89 行  

7. **空地与地形判定**  
   - `Core_List::is_BuildingCanBuild(int buildtype, int BlockDR, int BlockUR, int playerID, QString& chineseName)`：[Core_List.cpp](Core_List.cpp) 589-656 行  
   - 越界、未探索、`theMap->isHaveObject`（重叠）、`theMap->isFlat`（非船坞）、陆地/海洋、船坞用 `canBuildDock`。

---

## 五、config.json 中与建造相关的键（节选）

以下为**建造建筑**时用到的资源与时间（仅列出“造建筑”本身，不含建筑内科技/造兵）：

```text
INITIAL_WOOD, INITIAL_MEAT, INITIAL_GOLD, INITIAL_STONE   // 初始资源
BUILD_CENTER_WOOD, TIME_BUILD_CENTER
BUILD_HOUSE_WOOD, TIME_BUILD_HOME
BUILD_STOCK_WOOD, TIME_BUILD_STOCK
BUILD_GRANARY_WOOD, TIME_BUILD_GRANARY
BUILD_ARMYCAMP_WOOD, TIME_BUILD_ARMYCAMP
BUILD_DOCK_WOOD, TIME_BUILD_DOCK
BUILD_MARKET_WOOD, TIME_BUILD_MARKET
BUILD_RANGE_WOOD, TIME_BUILD_RANGE
BUILD_STABLE_WOOD, TIME_BUILD_STABLE
BUILD_ARROWTOWER_STONE, TIME_BUILD_ARROWTOWER
BUILD_FARM_WOOD, TIME_BUILD_FARM
BUILD_SIEGE_WOOD, TIME_BUILD_SIEGE
BUILD_COLLAGE_WOOD, TIME_BUILD_COLLAGE
```

修改这些键的值会改变村民建造时的消耗与建造时间（需在程序启动时通过 config 加载到全局变量）。

---

## 六、具体代码摘录

以下为村民建造流程中涉及的**实际代码**，按调用顺序列出。

**说明**：前面 6.1–6.6 多为**调用链**（例如 UI/AI 调 Core，Core 调 Core_List、Player、Development）；**真正做“能否造、扣多少”判断的是内核**，在 **6.8 节**（GlobalVariate.h 的 `conditionDevelop` + Player 的 `changeResource`）。Development.h 里的 `get_isBuildingShowAble` 等只是转发到 `buildCon->isShowable(...)`，不做判断逻辑。

### 6.1 AI 下达建造指令（AI.cpp）

```cpp
int AI::HumanBuild(int SN, int BuildingNum, int BlockDR, int BlockUR){
    return AddToIns(instruction(INS_HUMANBUILD, SN, BlockDR, BlockUR, BuildingNum));
}
```

### 6.2 每帧执行 AI 建造指令（Core.cpp）

```cpp
case INS_HUMANBUILD:    // 命令村民 self 在块坐标 BlockL,BlockU 处建造类型为 option 的新建筑
{
    if (self->getSort() != SORT_FARMER) {
        ret = ACTION_INVALID_SN;
        break;
    }
    Farmer* farmer = (Farmer*)self;
    if (farmer->get_farmerType() != FARMERTYPE_FARMER) {
        ret = ACTION_INVALID_ACTION;
        break;
    }
    if (cur.BlockDR < 0 || cur.BlockUR < 0 || cur.BlockDR >= MAP_L || cur.BlockUR >= MAP_U) {
        ret = ACTION_INVALID_LOCATION;
        break;
    }
    if (cur.option < 0 || cur.option > BUILDING_TYPE_MAXNUM) {
        ret = ACTION_INVALID_BUILDINGNUM;
        break;
    }
    ret = interactionList->addRelation(self, cur.BlockDR, cur.BlockUR, CoreEven_CreatBuilding, true, cur.option);
    // ...
}
break;
```

### 6.3 玩家点击建造按钮（SelectWidget.cpp）

```cpp
void SelectWidget::getBuild(int BlockL, int BlockU, int num)
{
    core->addRelation(nowobject, BlockL, BlockU, CoreEven_CreatBuilding, true, num);
}
```

### 6.4 关系表：添加建造并扣资源、创建地基（Core_List.cpp）

```cpp
int Core_List::addRelation(Coordinate* object1, int BlockDR, int BlockUR, int eventType, bool respond, int type)
{
    if (object1 == NULL) return ACTION_INVALID_NULLWORKER;
    if (object1->getSort() != SORT_FARMER) return ACTION_INVALID_SN;
    if (((Farmer*)object1)->get_farmerType() != FARMERTYPE_FARMER) return ACTION_INVALID_SN;

    if (relate_AllObject[object1].isExist && relate_AllObject[object1].respondConduct) suspendRelation(object1);

    if (!relate_AllObject[object1].isExist)
    {
        // ...
        if (eventType == CoreEven_CreatBuilding)
        {
            int playerRepresent = object1->getPlayerRepresent();
            int wrongCode = is_BuildingCanBuild(type, BlockDR, BlockUR, playerRepresent, chineseName);
            if (wrongCode < 0) return wrongCode;

            if (!player[playerRepresent]->get_isBuildingShowAble(type))
                return ACTION_INVALID_HUMANBUILD_LOCK;

            if (!player[playerRepresent]->get_isBuildingAble(type))
                return ACTION_INVALID_RESOURCE;

            player[playerRepresent]->changeResource_byBuild(type);
            buildOb = player[playerRepresent]->addBuilding(type, BlockDR, BlockUR);
            theMap->add_Map_Object(buildOb);
            return addRelation(object1, buildOb, CoreEven_FixBuilding, respond);
        }
    }
    return ACTION_INVALID_ISNTFREE;
}
```

### 6.5 空地/地形判定（Core_List.cpp）

```cpp
int Core_List::is_BuildingCanBuild(int buildtype, int BlockDR, int BlockUR, int playerID, QString& chineseName)
{
    // 预创建临时建筑取占地与中文名
    if (buildtype == BUILDING_FARM)
        tempBuild = new Building_Resource(buildtype, BlockDR, BlockUR);
    else
        tempBuild = new Building(buildtype, BlockDR, BlockUR);
    blockSideLength = tempBuild->get_BlockSizeLen();
    chineseName = tempBuild->getChineseName();
    delete tempBuild;

    // 越界
    if (DRL<0 || DRR>MAP_L || URD<0 || URU>MAP_U)
        return ACTION_INVALID_HUMANBUILD_OVERBORDER;
    // 未探索
    if (!theMap->cell[DRL][URD].Explored)
        return ACTION_INVALID_HUMANBUILD_UNEXPLORE;
    // 重叠
    if (theMap->isHaveObject(BlockDR, BlockUR, bDR_ba, bUR_ba, blockSideLength))
        return ACTION_INVALID_HUMANBUILD_OVERLAP;
    // 非船坞要求平坦
    if (buildtype != BUILDING_DOCK && !theMap->isFlat(BlockDR, BlockUR, blockSideLength))
        return ACTION_INVALID_HUMANBUILD_DIFFERENTHIGH;
    // 船坞靠水 / 其余建筑陆地
    if (buildtype == BUILDING_DOCK) {
        if (!canBuildDock(BlockDR, BlockUR, blockSideLength)) return ACTION_INVALID_POSITION_NOT_FIT;
    } else {
        for (int i = 0; i < blockSideLength; ++i)
            for (int j = 0; j < blockSideLength; ++j)
                if (theMap->cell[l][u].getMapType() == MAPTYPE_OCEAN) return ACTION_INVALID_POSITION_NOT_FIT;
    }
    return ACTION_SUCCESS;
}
```

### 6.6 扣资源与创建建筑对象（Player.cpp）

```cpp
void Player::changeResource_byBuild(int buildNum)
{
    int wood, food, stone, gold;
    playerScience->get_Resource_Consume(buildNum, wood, food, stone, gold);
    changeResource(wood, food, stone, gold, true);  // true 表示扣除
}

Building* Player::addBuilding(int Num, int BlockDR, int BlockUR, double percent)
{
    Building *newbuilding = NULL;
    if (Num == BUILDING_FARM)
        newbuilding = new Building_Resource(Num, BlockDR, BlockUR, getCiv(), playerScience, represent, percent);
    else
        newbuilding = new Building(Num, BlockDR, BlockUR, getCiv(), playerScience, represent, percent);
    build.push_back(newbuilding);
    return newbuilding;
}
```

### 6.7 是否显示/可造与资源消耗：调用链（仅转发）

下面这些是**封装/调用层**，只做转发，不做判断逻辑：

```cpp
// Player.h —— 只是转发给 Development
bool get_isBuildingShowAble(int buildNum) { return playerScience->get_isBuildingShowAble(buildNum, getCiv()); }
bool get_isBuildingAble(int buildNum)      { return playerScience->get_isBuildingAble(buildNum, wood, food, stone, gold); }

// Development.h —— 只是转发给 developLab[].buildCon（即 conditionDevelop*）
bool get_isBuildingShowAble(int buildingNum, int civilization) {
    return developLab[buildingNum].buildCon->isShowable(civilization);
}
bool get_isBuildingAble(int buildingNum, int wood, int food, int stone, int gold) {
    return developLab[buildingNum].buildCon->executable(wood, food, stone, gold);
}
void get_Resource_Consume(int buildNum, int& wood, int& food, int& stone, int& gold) {
    developLab[buildNum].buildCon->get_needResource(wood, food, stone, gold);
}
```

**真正做判断和读资源的是内核**，见下一节。

### 6.8 内核实现：conditionDevelop 与扣资源（GlobalVariate.h / Player.cpp）

建造的**时代/前置/资源是否足够/扣多少资源**，最终都在下面这些逻辑里完成，而不是在 Development/Player 的封装函数里。

**（1）内核：conditionDevelop 结构体与判断逻辑**（[GlobalVariate.h](GlobalVariate.h) 约 1106–1190 行）

```cpp
struct conditionDevelop
{
    int civilization;           // 要求的最低时代
    int sort_building;          // 所属建筑类型
    double times_second;        // 建造/行动耗时（秒）
    int acttimes = 0;           // 该条件已被“完成”的次数（建造完成一次 +1）
    list<conditionDevelop*> preCondition;  // 前置条件（如市场依赖谷仓）
    int need_Wood, need_Food, need_Stone, need_Gold;

    conditionDevelop(int civilization, int sort_building, double needTimes,
                     int need_Wood = 0, int need_Food = 0, int need_Stone = 0, int need_Gold = 0)
    {
        this->civilization = civilization;
        this->sort_building = sort_building;
        this->need_Wood = need_Wood;
        this->need_Food = need_Food;
        this->need_Stone = need_Stone;
        this->need_Gold = need_Gold;
        this->times_second = needTimes;
    }

    void addPreCondition(conditionDevelop* con_need) { preCondition.push_back(con_need); }

    // 内核：资源是否足够（直接比较数值）
    bool executable(int wood, int food, int stone, int gold) {
        return wood >= need_Wood && food >= need_Food && stone >= need_Stone && gold >= need_Gold;
    }

    // 内核：读出消耗（直接返回成员变量，这些值来自构造函数里传入的全局常量）
    void get_needResource(int& wood, int& food, int& stone, int& gold) {
        wood = need_Wood; food = need_Food; stone = need_Stone; gold = need_Gold;
    }

    // 内核：是否显示/可造 = 时代达标 + 所有前置条件的 acttimes > 0（即前置已“完成”过）
    bool isShowable(int nowcivilization)
    {
        if (civilization > nowcivilization) return false;
        for (list<conditionDevelop*>::iterator iter = preCondition.begin(); iter != preCondition.end(); iter++)
            if (!(*iter)->acttimes) return false;   // 前置必须至少完成过一次
        return true;
    }

    void finishAct() { acttimes++; }   // 建造完成时调用，用于前置判断
};
```

- 对**建筑建造**而言，用的是 `developLab[建筑].buildCon`，即一个 `conditionDevelop*`。  
- **是否显示**：`buildCon->isShowable(当前时代)`：当前时代 ≥ buildCon 的 civilization，且每个 `preCondition` 的 `acttimes > 0`（例如市场的 preCondition 是谷仓的 buildCon，谷仓建过一次后其 buildCon->acttimes 才会被加 1）。  
- **资源是否足够**：`buildCon->executable(wood, food, stone, gold)`：四个资源分别 ≥ need_*。  
- **扣资源时读出的数量**：`buildCon->get_needResource(...)` 就是把 need_Wood 等四个成员赋给引用参数，这些值在 `init_DevelopLab()` 里用 `BUILD_*_WOOD` 等全局常量构造时传入。

**（2）建造完成时“前置计数”如何 +1**

建筑造好后会调用 `Player::finishBuild(buildBuilding)`，内部调用 `playerScience->finishAction(buildBuilding->getNum())`，即 `developLab[建筑类型].finishBuild()`，最终执行的是 **buildCon->finishAct()**，即把该建筑的 `buildCon->acttimes++`。这样依赖它的建筑（preCondition 指向该 buildCon）的 `isShowable` 才会通过。

**（3）内核：实际扣资源**（[Player.cpp](Player.cpp)）

```cpp
// 真正做加减的是 changeResource；negative == true 表示扣资源
void Player::changeResource(int wood, int food, int stone, int gold, bool negative)
{
    if (negative) wood *= -1, food *= -1, stone *= -1, gold *= -1;
    this->wood  += wood;
    this->food  += food;
    this->stone += stone;
    this->gold  += gold;
}

// changeResource_byBuild 只是从内核读出消耗，再交给 changeResource 扣
void Player::changeResource_byBuild(int buildNum)
{
    int wood, food, stone, gold;
    playerScience->get_Resource_Consume(buildNum, wood, food, stone, gold);  // 调用 buildCon->get_needResource
    changeResource(wood, food, stone, gold, true);
}
```

总结：**“是否可造”和“扣多少”由 GlobalVariate.h 的 conditionDevelop（isShowable、executable、get_needResource）决定；Player 只负责用 changeResource 做数值加减。** Development / Player 的 get_isBuildingShowAble、get_isBuildingAble、get_Resource_Consume 只是转发到这些内核逻辑。

### 6.9 科技树：各建筑建造条件（Development.cpp init_DevelopLab）

```cpp
// 市镇中心：铁器时代，200 木，60 秒
developLab[BUILDING_CENTER].buildCon = new conditionDevelop(CIVILIZATION_IRONAGE, BUILDING_CENTER, TIME_BUILD_CENTER, BUILD_CENTER_WOOD);

// 房屋：石器时代，30 木，20 秒
developLab[BUILDING_HOME].buildCon = new conditionDevelop(CIVILIZATION_STONEAGE, BUILDING_HOME, TIME_BUILD_HOME, BUILD_HOUSE_WOOD);

// 仓库：石器时代，120 木，30 秒
developLab[BUILDING_STOCK].buildCon = new conditionDevelop(CIVILIZATION_STONEAGE, BUILDING_STOCK, TIME_BUILD_STOCK, BUILD_STOCK_WOOD);

// 谷仓：石器时代，120 木，30 秒
developLab[BUILDING_GRANARY].buildCon = new conditionDevelop(CIVILIZATION_STONEAGE, BUILDING_GRANARY, TIME_BUILD_GRANARY, BUILD_GRANARY_WOOD);

// 军营：石器时代，125 木，30 秒
developLab[BUILDING_ARMYCAMP].buildCon = new conditionDevelop(CIVILIZATION_STONEAGE, BUILDING_ARMYCAMP, TIME_BUILD_ARMYCAMP, BUILD_ARMYCAMP_WOOD);

// 市场：工具时代，150 木，40 秒，前置谷仓
developLab[BUILDING_MARKET].buildCon = new conditionDevelop(CIVILIZATION_TOOLAGE, BUILDING_MARKET, TIME_BUILD_MARKET, BUILD_MARKET_WOOD);
developLab[BUILDING_MARKET].buildCon->addPreCondition(developLab[BUILDING_GRANARY].buildCon);

// 靶场：工具时代，150 木，40 秒，前置军营
developLab[BUILDING_RANGE].buildCon = new conditionDevelop(CIVILIZATION_TOOLAGE, BUILDING_RANGE, TIME_BUILD_RANGE, BUILD_RANGE_WOOD);
developLab[BUILDING_RANGE].buildCon->addPreCondition(developLab[BUILDING_ARMYCAMP].buildCon);

// 马厩：工具时代，150 木，40 秒，前置军营
developLab[BUILDING_STABLE].buildCon = new conditionDevelop(CIVILIZATION_TOOLAGE, BUILDING_STABLE, TIME_BUILD_STABLE, BUILD_STABLE_WOOD);
developLab[BUILDING_STABLE].buildCon->addPreCondition(developLab[BUILDING_ARMYCAMP].buildCon);

// 箭塔：工具时代，0 木 0 食 150 石，80 秒，前置谷仓“研发箭塔”
developLab[BUILDING_ARROWTOWER].buildCon = new conditionDevelop(CIVILIZATION_TOOLAGE, BUILDING_ARROWTOWER, TIME_BUILD_ARROWTOWER, 0, 0, BUILD_ARROWTOWER_STONE);
developLab[BUILDING_ARROWTOWER].buildCon->addPreCondition(developLab[BUILDING_GRANARY].actCon[BUILDING_GRANARY_ARROWTOWER].headAct);

// 农田：工具时代，75 木，30 秒，前置市场
developLab[BUILDING_FARM].buildCon = new conditionDevelop(CIVILIZATION_TOOLAGE, BUILDING_FARM, TIME_BUILD_FARM, BUILD_FARM_WOOD);
developLab[BUILDING_FARM].buildCon->addPreCondition(developLab[BUILDING_MARKET].buildCon);

// 船坞：石器时代，100 木，40 秒
developLab[BUILDING_DOCK].buildCon = new conditionDevelop(CIVILIZATION_STONEAGE, BUILDING_DOCK, TIME_BUILD_DOCK, BUILD_DOCK_WOOD);

// 攻城武器厂：铜器时代，180 木，40 秒
developLab[BUILDING_SIEGE].buildCon = new conditionDevelop(CIVILIZATION_BRONZEAGE, BUILDING_SIEGE, TIME_BUILD_SIEGE, BUILD_SIEGE_WOOD, 0, 0, 0);

// 学院：铜器时代，180 木，40 秒
developLab[BUILDING_COLLAGE].buildCon = new conditionDevelop(CIVILIZATION_BRONZEAGE, BUILDING_COLLAGE, TIME_BUILD_COLLAGE, BUILD_COLLAGE_WOOD, 0, 0, 0);
```

`conditionDevelop` 构造函数参数顺序为：**时代、建筑类型、建造时间(秒)、木材、食物、石头、金子**。这些参数在运行时来自 [config.json](config.json) 或 [GlobalVariate.cpp](GlobalVariate.cpp) 中的全局常量（如 `BUILD_CENTER_WOOD`）；构造函数会把它们写入 `conditionDevelop` 的成员（need_Wood 等），之后的 **executable / get_needResource / isShowable** 都只读这些成员，即**内核只依赖 conditionDevelop 自身状态**，不再回头调 Development。
