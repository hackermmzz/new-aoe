# SelectWidget and Command Panel

<details>
<summary>Relevant source files</summary>

The following files were used as context for generating this wiki page:

- [MainWidget.cpp](MainWidget.cpp)
- [MainWidget.h](MainWidget.h)
- [SelectWidget.cpp](SelectWidget.cpp)

</details>



The SelectWidget and Command Panel system provides the player interface for viewing selected entity information and issuing commands. This system displays unit/building statistics, manages the 12 action buttons that appear when entities are selected, and dispatches player commands to the game Core.

For AI command processing, see [Instruction and Command System](#5.2). For the rendering canvas, see [Rendering and Display](#4.3).

---

## System Overview

SelectWidget is a Qt widget located in the lower-left corner of the game interface that displays information about the currently selected game entity (building, unit, or resource). Below it are 12 action buttons (`ActWidget` instances) that change based on what is selected, allowing the player to train units, research technologies, construct buildings, or issue special commands.

```mermaid
graph TB
    subgraph "UI Layer"
        SEL["SelectWidget<br/>Entity Info Display"]
        ACT["ActWidget[12]<br/>Action Buttons"]
        UI_LABELS["QLabel Widgets<br/>objName/objHp/objText<br/>objIcon/objText_ATK/etc"]
    end
    
    subgraph "Data Arrays"
        ACTIONS["actions[12]<br/>ACT_* constants"]
        STATUS["actionStatus[12]<br/>ENABLED/DISABLED"]
        RESMAP["actionResourceMap<br/>Action → Icon mapping"]
    end
    
    subgraph "Action Flow"
        INIT["initActs()<br/>Populate actions[] from entity"]
        REFRESH["refreshActs()<br/>Update actionStatus[] from game state"]
        UPDATE["updateActs()<br/>Update building action states"]
        DRAW["drawActs()<br/>Render button icons"]
        WIDGET["widgetAct(num)<br/>Button click handler"]
        DO["doActs(actName)<br/>Execute action"]
    end
    
    subgraph "Game Integration"
        CORE["Core Engine"]
        PLAYER["Player[0]<br/>Resources/Tech"]
        AI["UsrAI::BuildingAction()"]
    end
    
    SEL --> UI_LABELS
    SEL --> ACT
    ACT --> WIDGET
    
    INIT --> ACTIONS
    REFRESH --> STATUS
    DRAW --> RESMAP
    
    ACTIONS --> DRAW
    STATUS --> DRAW
    DRAW --> ACT
    
    WIDGET --> DO
    DO --> CORE
    DO --> AI
    
    REFRESH --> PLAYER
    UPDATE --> PLAYER
    
    style SEL fill:#f9f9f9
    style ACT fill:#e8e8e8
    style DO fill:#d0d0d0
```

**Sources:** [SelectWidget.cpp:1-1333](), [SelectWidget.h:1-82](), [MainWidget.cpp:1338-1370]()

---

## SelectWidget Display Components

The `SelectWidget` class displays detailed information about the currently selected entity using multiple QLabel widgets arranged in a grid layout.

### Display Elements

| UI Element | Variable | Purpose | Position |
|------------|----------|---------|----------|
| Object Icon | `ui->objIcon` | 110x110 entity icon | Main display |
| Object Name | `ui->objName` | Entity name (Chinese) | Above icon |
| HP Display | `ui->objHp` | "Current/Max" HP | Below name |
| Progress Text | `ui->objText` | Progress % or resource count | Center |
| Small Icon | `ui->objIconSmall` | Resource type or status icon | Next to progress |
| Attack Display | `ui->objText_ATK` + `ui->objIconSmall_ATK` | Attack value | Stats row 1 |
| Melee Defense | `ui->objText_DEF_melee` + `ui->objIconSmall_DEF_melee` | Melee defense value | Stats row 2 |
| Range Defense/Range | `ui->objText_DEF_range` + `ui->objIconSmall_DEF_range` | Ranged defense or range | Stats row 3 |

### Entity-Specific Display Logic

```mermaid
graph LR
    subgraph "Building Display"
        B_ICON["Building Icon<br/>Based on civ + type"]
        B_HP["HP Display"]
        B_PROGRESS["Construction %<br/>or Action %"]
        B_SPECIAL["Special: House shows<br/>population N/M<br/>Farm shows food count"]
    end
    
    subgraph "Unit Display - Farmer"
        F_ICON["Farmer/Boat Icon"]
        F_HP["HP Display"]
        F_RES["Resource Carried<br/>Icon + Count"]
        F_ATK["Attack Stats<br/>for combat state"]
    end
    
    subgraph "Unit Display - Army"
        A_ICON["Army Icon<br/>by type + level"]
        A_HP["HP Display"]
        A_ATK["ATK: base+bonus"]
        A_DEF_M["Melee DEF: base+bonus"]
        A_DEF_R["Range DEF or Range<br/>Archers show range"]
    end
    
    subgraph "Resource Display"
        R_ICON["Resource Icon<br/>Tree/Stone/Gold"]
        R_COUNT["Remaining Amount"]
        R_TYPE["Resource Type Icon"]
    end
```

**Display Update:** The `refreshActs()` function updates all display elements every frame based on `nowobject` type.

**Sources:** [SelectWidget.cpp:106-145](), [SelectWidget.cpp:546-905]()

---

## Command Panel Architecture

The command panel consists of 12 `ActWidget` instances initialized in `MainWidget::initInfoPane()` and managed by `SelectWidget`. These buttons are arranged in a 6×2 grid at the bottom-right of the screen.

### ActWidget Array Initialization

```mermaid
graph TB
    subgraph "MainWidget Initialization"
        INIT["initInfoPane()"]
        ACTS_PTR["acts[12] global array"]
        GRID["ui->gridLayout<br/>6 columns × 2 rows"]
    end
    
    subgraph "ActWidget Setup"
        SIZE["setFixedSize(70, 70)"]
        HIDE["hide() initially"]
        STATUS["setStatus(0)<br/>disabled by default"]
        CONNECT["connect to SelectWidget::widgetAct()"]
    end
    
    INIT --> ACTS_PTR
    INIT --> SIZE
    INIT --> HIDE
    INIT --> STATUS
    INIT --> CONNECT
    ACTS_PTR --> GRID
    
    style INIT fill:#f0f0f0
```

Each `ActWidget` has:
- **num**: Button index (0-11)
- **status**: `ACT_STATUS_ENABLED` or `ACT_STATUS_DISABLED` (affects visual rendering)
- **pix**: QPixmap icon loaded from `resMap`
- **actPress(int)** signal: Emitted when clicked

**Sources:** [MainWidget.cpp:1338-1370](), [SelectWidget.cpp:1-19]()

---

## Action System Data Structures

The action system uses three parallel arrays to manage the 12 command buttons:

### Core Data Arrays

```cpp
// In SelectWidget class
int actions[ACT_WINDOW_NUM_FREE];        // Which action each button represents
int actionStatus[ACT_WINDOW_NUM_FREE];   // Whether button is enabled
QMap<int, QString> actionResourceMap;    // Maps action ID to icon resource name
```

### Action Flow State Machine

```mermaid
stateDiagram-v2
    [*] --> EntitySelected: Click entity
    
    EntitySelected --> InitActions: initActs()
    InitActions --> PopulateActions: Set actions[] based on entity type
    
    PopulateActions --> FrameLoop: Enter update loop
    
    state FrameLoop {
        [*] --> RefreshStatus
        RefreshStatus --> CheckResources: refreshActs()
        CheckResources --> UpdateStatus: Update actionStatus[]
        UpdateStatus --> DrawButtons: drawActs()
        DrawButtons --> RenderIcons: Load icons from actionResourceMap
        RenderIcons --> [*]
    }
    
    FrameLoop --> ButtonClick: User clicks button
    ButtonClick --> DispatchAction: widgetAct(num)
    DispatchAction --> ExecuteAction: doActs(actions[num])
    ExecuteAction --> UpdateActions: Action may trigger initActs()
    UpdateActions --> FrameLoop
    
    EntitySelected --> [*]: Deselect
```

**Sources:** [SelectWidget.cpp:154-261](), [SelectWidget.cpp:263-544](), [SelectWidget.cpp:1213-1230](), [SelectWidget.cpp:1232-1326]()

---

## Action Initialization (initActs)

The `initActs()` function populates the `actions[]` array based on the selected entity type. This determines which commands appear on the command panel.

### Entity-Specific Action Sets

| Entity Type | Action Sources | Special Logic |
|-------------|----------------|---------------|
| `SORT_BUILDING` | `Building::getActNames(i)` | Shows STOP if building is acting; otherwise shows building-specific actions |
| `SORT_FARMER` | Hardcoded: `ACT_BUILD` (position 0), `ACT_SHIP_LAY` for transport ships | Farmers can construct buildings |
| `SORT_ARMY` | For `AT_STONE_THROWER`: `ACT_STONE_THROWER_PINPOINT_STRIKE` or `ACT_STONE_THROWER_CANCEL_PINPOINT_STRIKE` | Other army units have no actions |
| Others | All `ACT_NULL` | Resources and animals have no actions |

### Building Action Visibility

Buildings use a two-tier visibility system:
1. **Technology Visibility**: `Player::get_isBuildActionShowAble()` checks if tech is researched
2. **Resource Availability**: Checked in `refreshActs()` for graying out buttons

```mermaid
graph TD
    START["initActs() called"]
    
    CHECK_TYPE{"nowobject->getSort()"}
    
    BUILDING["Entity is Building"]
    FARMER["Entity is Farmer"]
    ARMY["Entity is Army"]
    OTHER["Other entity type"]
    
    B_ACTING{"building->isFinish() &&<br/>building->getActSpeed() > 0"}
    B_SHOW_STOP["actions[0] = ACT_STOP"]
    B_SHOW_ACTIONS["Loop i=0 to 11:<br/>actions[i] = building->getActNames(i)<br/>if get_isBuildActionShowAble()"]
    
    F_SHIP{"farmer->isShip() &&<br/>get_farmerType() == 1"}
    F_SHOW_LAY["actions[0] = ACT_SHIP_LAY"]
    F_SHOW_BUILD["actions[0] = ACT_BUILD"]
    
    A_STONE{"army->getNum() ==<br/>AT_STONE_THROWER"}
    A_WAITING{"isWaitingForPinPointStrike()"}
    A_SHOW_CANCEL["actions[0] = ACT_STONE_THROWER_CANCEL_PINPOINT_STRIKE"]
    A_SHOW_STRIKE["actions[0] = ACT_STONE_THROWER_PINPOINT_STRIKE"]
    A_NO_ACTIONS["All actions[i] = ACT_NULL"]
    
    O_NO_ACTIONS["All actions[i] = ACT_NULL"]
    
    START --> CHECK_TYPE
    
    CHECK_TYPE -->|SORT_BUILDING| BUILDING
    CHECK_TYPE -->|SORT_FARMER| FARMER
    CHECK_TYPE -->|SORT_ARMY| ARMY
    CHECK_TYPE -->|Other| OTHER
    
    BUILDING --> B_ACTING
    B_ACTING -->|Yes| B_SHOW_STOP
    B_ACTING -->|No| B_SHOW_ACTIONS
    
    FARMER --> F_SHIP
    F_SHIP -->|Yes| F_SHOW_LAY
    F_SHIP -->|No| F_SHOW_BUILD
    
    ARMY --> A_STONE
    A_STONE -->|Yes| A_WAITING
    A_STONE -->|No| A_NO_ACTIONS
    A_WAITING -->|Yes| A_SHOW_CANCEL
    A_WAITING -->|No| A_SHOW_STRIKE
    
    OTHER --> O_NO_ACTIONS
```

**Sources:** [SelectWidget.cpp:154-261]()

---

## Action Status Refresh (refreshActs)

The `refreshActs()` function runs every frame to update button enabled/disabled states and the information display. This is separate from `initActs()` to avoid recalculating action arrays unnecessarily.

### Status Update Logic

```mermaid
graph TB
    START["refreshActs() called every frame"]
    
    UPDATE_DISPLAY["Update entity display<br/>objName, objHp, objIcon, etc."]
    
    LOOP["For i=0 to 11"]
    
    CHECK_ACTION{"actions[i] type"}
    
    BUILD_ACTION["Building Construction<br/>(ACT_BUILD_*)"]
    BUILD_ACT["Building Action<br/>(ACT_UPGRADE_*, ACT_*_CREATE_*)"]
    SPECIAL["Special Actions<br/>(ACT_NULL, ACT_STOP, etc.)"]
    
    CHECK_BUILD_VIS{"Player::get_isBuildingShowAble(buildType) &&<br/>Player::get_isBuildingAble(buildType)"}
    SET_BUILD_ENABLED["actionStatus[i] = ACT_STATUS_ENABLED"]
    SET_BUILD_DISABLED["actionStatus[i] = ACT_STATUS_DISABLED"]
    
    CHECK_ACT_VIS{"Player::get_isBuildActionAble(buildType, actType)"}
    SET_ACT_ENABLED["actionStatus[i] = ACT_STATUS_ENABLED"]
    SET_ACT_DISABLED["actionStatus[i] = ACT_STATUS_DISABLED"]
    
    SET_SPECIAL["Set special status"]
    
    APPLY["mainPtr->getActs(i)->setStatus(actionStatus[i])<br/>mainPtr->getActs(i)->update()"]
    
    START --> UPDATE_DISPLAY
    UPDATE_DISPLAY --> LOOP
    LOOP --> CHECK_ACTION
    
    CHECK_ACTION -->|Build| BUILD_ACTION
    CHECK_ACTION -->|BuildingAct| BUILD_ACT
    CHECK_ACTION -->|Other| SPECIAL
    
    BUILD_ACTION --> CHECK_BUILD_VIS
    CHECK_BUILD_VIS -->|Yes| SET_BUILD_ENABLED
    CHECK_BUILD_VIS -->|No| SET_BUILD_DISABLED
    
    BUILD_ACT --> CHECK_ACT_VIS
    CHECK_ACT_VIS -->|Yes| SET_ACT_ENABLED
    CHECK_ACT_VIS -->|No| SET_ACT_DISABLED
    
    SPECIAL --> SET_SPECIAL
    
    SET_BUILD_ENABLED --> APPLY
    SET_BUILD_DISABLED --> APPLY
    SET_ACT_ENABLED --> APPLY
    SET_ACT_DISABLED --> APPLY
    SET_SPECIAL --> APPLY
    
    APPLY --> LOOP
```

### Resource Checks

The `Player::get_isBuildActionAble()` and `Player::get_isBuildingAble()` methods check:
- Wood, food, stone, gold availability
- Technology prerequisites (from Development system)
- Population capacity
- Building prerequisites (e.g., must have Market before researching market upgrades)

**Sources:** [SelectWidget.cpp:263-544](), [MainWidget.cpp:1213-1230]()

---

## Action Icon Rendering (drawActs)

The `drawActs()` function loads and displays the appropriate icon for each action button from the resource map.

### Icon Selection Logic

```mermaid
graph TD
    START["drawActs() called"]
    LOOP["For i=0 to 11"]
    
    CHECK_BUILD{"actions[i] is<br/>ACT_BUILD_* ?"}
    
    GET_BUILD_ICON["Get icon from Building::getBuiltname(civ, 0, buildingNum)<br/>Button_ + builtname"]
    
    CHECK_MAP{"actions[i] in<br/>actionResourceMap?"}
    
    GET_MAPPED["actionKey = actionResourceMap[actions[i]]"]
    
    CHECK_CLUBMAN{"actions[i] ==<br/>ACT_ARMYCAMP_CREATE_CLUBMAN"}
    
    GET_CLUBMAN_LEVEL{"get_buildActLevel(BUILDING_ARMYCAMP,<br/>BUILDING_ARMYCAMP_UPGRADE_CLUBMAN)"}
    CLUBMAN_0["actionKey = Button_Clubman"]
    CLUBMAN_1["actionKey = Button_Axeman"]
    
    DEFAULT["actionKey = Button"]
    
    LOAD{"resMap.find(actionKey)"}
    
    LOAD_PIX["pix = resMap[actionKey].front().scaled(70,70)"]
    LOAD_DEFAULT["pix = resMap[Button].front().scaled(70,70)"]
    
    SET_PIX["mainPtr->getActs(i)->setPix(pix)"]
    
    SHOW_HIDE{"actions[i] != ACT_NULL"}
    SHOW["mainPtr->getActs(i)->show()"]
    HIDE["mainPtr->getActs(i)->hide()"]
    
    UPDATE["mainPtr->getActs(i)->update()"]
    
    START --> LOOP
    LOOP --> CHECK_BUILD
    
    CHECK_BUILD -->|Yes| GET_BUILD_ICON
    CHECK_BUILD -->|No| CHECK_MAP
    
    GET_BUILD_ICON --> LOAD
    
    CHECK_MAP -->|Yes| GET_MAPPED
    CHECK_MAP -->|No| CHECK_CLUBMAN
    
    GET_MAPPED --> LOAD
    
    CHECK_CLUBMAN -->|Yes| GET_CLUBMAN_LEVEL
    CHECK_CLUBMAN -->|No| DEFAULT
    
    GET_CLUBMAN_LEVEL --> CLUBMAN_0
    GET_CLUBMAN_LEVEL --> CLUBMAN_1
    
    CLUBMAN_0 --> LOAD
    CLUBMAN_1 --> LOAD
    DEFAULT --> LOAD
    
    LOAD -->|Found| LOAD_PIX
    LOAD -->|Not found| LOAD_DEFAULT
    
    LOAD_PIX --> SET_PIX
    LOAD_DEFAULT --> SET_PIX
    
    SET_PIX --> SHOW_HIDE
    
    SHOW_HIDE -->|Yes| SHOW
    SHOW_HIDE -->|No| HIDE
    
    SHOW --> UPDATE
    HIDE --> UPDATE
    
    UPDATE --> LOOP
```

**Action Resource Map Sample:**
```cpp
actionResourceMap[ACT_CREATEFARMER] = "Button_Villager";
actionResourceMap[ACT_UPGRADE_AGE] = "ButtonTech_Center1";
actionResourceMap[ACT_BUILD] = "Button_Build";
actionResourceMap[ACT_STONE_THROWER_PINPOINT_STRIKE] = "Button_PinPointStrike";
```

**Sources:** [SelectWidget.cpp:21-77](), [SelectWidget.cpp:1232-1326]()

---

## Action Execution (doActs)

The `doActs()` function is the central dispatcher that executes player commands. It is called when a button is clicked (`widgetAct()`) or when AI issues a command (`aiAct()`).

### Execution Flow

```mermaid
sequenceDiagram
    participant User
    participant ActWidget
    participant SelectWidget
    participant doActs
    participant Core
    participant AI
    participant MainWidget
    
    User->>ActWidget: Click button num
    ActWidget->>SelectWidget: emit actPress(num)
    SelectWidget->>SelectWidget: widgetAct(num)
    
    alt Button is disabled
        SelectWidget->>SelectWidget: return (check getStatus())
    end
    
    SelectWidget->>SelectWidget: actName = actions[num]
    SelectWidget->>doActs: doActs(actName)
    
    alt Building Construction (ACT_BUILD_*)
        doActs->>MainWidget: Set cursor to building icon
        doActs->>MainWidget: emit sendBuildMode(buildingNum)
        Note over doActs,MainWidget: Enters build mode, waits for map click
    
    else Building Action (ACT_*_CREATE_*, ACT_UPGRADE_*)
        doActs->>AI: UsrAI::BuildingAction(globalNum, actionType)
        AI->>AI: Serialize to instruction struct
        AI->>Core: Queue instruction via ins
        
    else Special Commands
        alt ACT_SHIP_LAY
            doActs->>Core: addRelation(nowobject, CoreEven_UnLoad)
        else ACT_STONE_THROWER_PINPOINT_STRIKE
            doActs->>MainWidget: setWaitingForPinPointStrike(true)
            doActs->>MainWidget: setPinPointStrikeUnit(nowobject)
            Note over doActs,MainWidget: Waits for map click to get target
        else ACT_STONE_THROWER_CANCEL_PINPOINT_STRIKE
            doActs->>Core: suspendRelation(nowobject)
            doActs->>MainWidget: setWaitingForPinPointStrike(false)
        else ACT_STOP
            doActs->>Core: suspendRelation(nowobject)
        end
    end
    
    doActs->>SelectWidget: Update all ActWidgets
```

**Sources:** [SelectWidget.cpp:907-913](), [SelectWidget.cpp:915-918](), [SelectWidget.cpp:933-1192]()

---

## Building Mode System

When the player clicks `ACT_BUILD` or any building construction action, the system enters "build mode" where the cursor changes to the building icon and waits for a map click.

### Build Mode Flow

```mermaid
graph TB
    START["User clicks Build button"]
    
    BUILD_CLICK{"actions[num] ==<br/>ACT_BUILD"}
    
    SHOW_MENU["secondWidget_Build = true<br/>showBuildActLab()"]
    POPULATE["Replace actions[] with:<br/>ACT_BUILD_HOUSE, ACT_BUILD_GRANARY,<br/>ACT_BUILD_STOCK, etc.<br/>ACT_BUILD_CANCEL"]
    
    SPECIFIC{"User clicks specific<br/>building button"}
    
    SET_CURSOR["QApplication::setOverrideCursor()<br/>Load building icon as cursor"]
    SET_MODE["emit sendBuildMode(buildingNum)"]
    
    WAIT["Wait for map click"]
    
    MAP_CLICK["User clicks on map"]
    
    GET_BUILD["GameWidget receives click<br/>Calls SelectWidget::getBuild(L, U, num)"]
    
    ADD_RELATION["core->addRelation(nowobject, L, U,<br/>CoreEven_CreatBuilding, true, num)"]
    
    CANCEL{"User clicks<br/>ACT_BUILD_CANCEL"}
    
    RESTORE_CURSOR["QApplication::restoreOverrideCursor()"]
    RESTORE_MODE["emit sendBuildMode(-1)"]
    REINIT["initActs()<br/>Restore normal actions"]
    
    START --> BUILD_CLICK
    BUILD_CLICK -->|Yes| SHOW_MENU
    SHOW_MENU --> POPULATE
    POPULATE --> SPECIFIC
    
    SPECIFIC --> SET_CURSOR
    SET_CURSOR --> SET_MODE
    SET_MODE --> WAIT
    
    WAIT --> MAP_CLICK
    MAP_CLICK --> GET_BUILD
    GET_BUILD --> ADD_RELATION
    
    WAIT --> CANCEL
    CANCEL --> RESTORE_CURSOR
    RESTORE_CURSOR --> RESTORE_MODE
    RESTORE_MODE --> REINIT
```

### Building Availability (showBuildActLab)

The `showBuildActLab()` function populates the action panel with building options:

| Button Position | Building Type | Visibility Check |
|----------------|---------------|------------------|
| 0 | `BUILDING_HOME` | Always visible after researched |
| 1 | `BUILDING_GRANARY` | Requires Stone Age |
| 2 | `BUILDING_STOCK` | Requires Tool Age |
| 3 | `BUILDING_MARKET` | Requires market tech |
| 4 | `BUILDING_ARROWTOWER` | Requires tower tech |
| 5 | `BUILDING_ARMYCAMP` | Requires Tool Age |
| 6 | `BUILDING_RANGE` | Requires Tool Age |
| 7 | `BUILDING_STABLE` | Requires Tool Age |
| 8 | `BUILDING_FARM` | Requires farm tech |
| 10 | `BUILDING_COLLAGE` | Requires Bronze Age |
| 9 | `BUILDING_SIEGE` | Requires Bronze Age |
| 11 | `ACT_BUILD_CANCEL` | Always enabled |

**Sources:** [SelectWidget.cpp:963-1007](), [SelectWidget.cpp:1194-1211](), [SelectWidget.cpp:1329-1332]()

---

## Integration with Game Systems

### Connection to Core Engine

The SelectWidget interacts with the `Core` engine through:
- **Direct Relation Creation**: `core->addRelation()` for immediate actions
- **Relation Suspension**: `core->suspendRelation()` for stopping actions

### Connection to AI System

Player actions are processed through the AI system for validation and queuing:
- **Building Actions**: `UsrAI::BuildingAction(globalNum, actionType)` 
- **Unit Actions**: Future implementation
- This ensures player actions follow the same validation as AI commands

### Connection to Player Resources

The `Player` class provides multiple query functions used by SelectWidget:
- `get_isBuildingShowAble(buildType)`: Tech prerequisite check
- `get_isBuildingAble(buildType)`: Resource + tech check
- `get_isBuildActionAble(buildType, actionType)`: Action availability
- `get_isBuildActionShowAble(buildNum, actNum)`: Action visibility
- `get_buildActLevel(buildNum, actNum)`: Upgrade level (for icons)
- `getWood()`, `getFood()`, `getStone()`, `getGold()`: Resource quantities

### Frame Update Cycle

```mermaid
sequenceDiagram
    participant Timer
    participant MainWidget
    participant SelectWidget
    participant ActWidget
    
    Timer->>MainWidget: timeout() signal
    MainWidget->>SelectWidget: frameUpdate()
    
    SelectWidget->>SelectWidget: refreshActs()
    Note over SelectWidget: Update actionStatus[]<br/>Update entity display
    
    SelectWidget->>SelectWidget: updateActs()
    Note over SelectWidget: Update building action states
    
    SelectWidget->>SelectWidget: drawActs()
    Note over SelectWidget: Load and set button icons
    
    loop For each button
        SelectWidget->>ActWidget: setStatus(actionStatus[i])
        SelectWidget->>ActWidget: setPix(icon)
        SelectWidget->>ActWidget: show() or hide()
        SelectWidget->>ActWidget: update()
    end
```

**Sources:** [MainWidget.cpp:1378-1380](), [SelectWidget.cpp:147-152]()

---

## Key Constants and Enumerations

### Action Type Constants

Action types are defined in `config.h` and referenced throughout the system:

| Constant Prefix | Purpose | Example |
|----------------|---------|---------|
| `ACT_BUILD_*` | Building construction | `ACT_BUILD_HOUSE`, `ACT_BUILD_GRANARY` |
| `ACT_CREATEFARMER` | Unit creation from Center | Creates farmer unit |
| `ACT_UPGRADE_*` | Technology research | `ACT_UPGRADE_AGE`, `ACT_UPGRADE_WOOD` |
| `ACT_*_CREATE_*` | Unit training | `ACT_ARMYCAMP_CREATE_CLUBMAN` |
| `ACT_*_UPGRADE_*` | Unit upgrades | `ACT_ARMYCAMP_UPGRADE_CLUBMAN` |
| `ACT_STOP` | Stop current action | Cancels building/research |
| `ACT_BUILD` | Enter build mode | Shows building menu |
| `ACT_BUILD_CANCEL` | Exit build mode | Restores normal actions |
| `ACT_NULL` | No action assigned | Button is hidden |

### Action Status Values

```cpp
#define ACT_STATUS_ENABLED 1   // Button is clickable
#define ACT_STATUS_DISABLED 0  // Button is grayed out
```

**Sources:** [config.h]() (referenced), [SelectWidget.cpp:34-91]()

---

## Special Features

### Stone Thrower Pinpoint Strike

The stone thrower unit has a unique two-step action system:

1. **Activation**: Click `ACT_STONE_THROWER_PINPOINT_STRIKE`
   - Sets `MainWidget::waitingForPinPointStrike = true`
   - Button changes to `ACT_STONE_THROWER_CANCEL_PINPOINT_STRIKE`
   - Waits for map click

2. **Target Selection**: Click on map
   - Processed in `Core::manageMouseEvent()`
   - Creates `CoreEven_PinPointStrike` relation with target coordinates

3. **Cancellation**: Click `ACT_STONE_THROWER_CANCEL_PINPOINT_STRIKE`
   - Calls `core->suspendRelation(unit)`
   - Resets waiting state

**Sources:** [SelectWidget.cpp:1122-1167](), [MainWidget.h:52-60]()

### Dynamic Icon Selection by Civilization

Building construction buttons display different icons based on the player's current civilization age:

```cpp
// Example from drawActs()
int civ = mainPtr->player[0]->getCiv();  // 1=Stone, 2=Tool, 3=Bronze
string base = Building::getBuiltname(civ, 0, buildingNum);
string key = "Button_" + base;
```

This ensures that a Tool Age house shows the wooden building icon, while a Bronze Age house shows the brick building icon.

**Sources:** [SelectWidget.cpp:1243-1276]()

### Clubman Upgrade Level Icons

The clubman training button shows different icons based on the upgrade level:
- Level 0: `Button_Clubman` (basic clubman)
- Level 1: `Button_Axeman` (upgraded to axeman)

This is queried via `Player::get_buildActLevel(BUILDING_ARMYCAMP, BUILDING_ARMYCAMP_UPGRADE_CLUBMAN)`.

**Sources:** [SelectWidget.cpp:1281-1296]()